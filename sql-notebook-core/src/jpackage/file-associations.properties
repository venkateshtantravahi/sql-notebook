# jpackage file-type association for .sqlnb notebooks.
# Used by the jpackage Gradle task to register the file type with the OS.
# On macOS this maps to CFBundleDocumentTypes in Info.plist.
# On Linux this maps to a MIME-type / .desktop association.

mime-type=application/x-sqlnotebook
extension=sqlnb
description=SQL Notebook
